info:
  name: Comment List
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/user/comments"
  params:
    - name: cursor
      value: ""
      type: query
      disabled: true
    - name: limit
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
