info:
  name: Detail
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/posts/{{postId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
