info:
  name: Logout
  type: http
  seq: 3

http:
  method: GET
  url: ""
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: bru.deleteVar("token")

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
