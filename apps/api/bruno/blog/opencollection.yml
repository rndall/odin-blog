opencollection: 1.0.0

info:
  name: odin-blog
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  variables:
    - name: postId
      value: "1"
  scripts:
    - type: before-request
      code: |-
        const token = bru.getEnvVar("token")

        if (!token) return

        req.setHeader("Authorization", `Bearer ${token}`)
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
