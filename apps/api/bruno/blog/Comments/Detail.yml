info:
  name: Detail
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/posts/{{postId}}/comments/{{commentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
