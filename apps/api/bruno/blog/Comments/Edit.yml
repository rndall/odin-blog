info:
  name: Edit
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/posts/{{postId}}/comments/{{commentId}}"
  body:
    type: json
    data: |-
      {
        "content": "Edited comment"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
